"""
UI subpackage.

Contains interactive controls, parameter displays, graphs, and HUD elements.
All UI reads from the simulation state; it never computes physics.

Modules:
    controls    — Interactive parameter sliders and buttons (Taichi GUI)
    overlays    — Educational text overlays and annotations
    hud         — Heads-up display showing real-time physical quantities
    graphs      — Real-time data plots (mass, luminosity, temperature vs time)
"""
